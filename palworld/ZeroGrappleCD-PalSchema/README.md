# ZeroGrappleCD (PalSchema)

Server half: set `CoolDownTime` / `NearCoolTimeRate` to `0` on all grappling gun BPs.

## Install
```
.../ue4ss/Mods/PalSchema/mods/ZeroGrappleCD/
  blueprints/Grappling Guns.jsonc
```
Restart dedicated server. Also install client Lua `ZeroGrappleCD` for Game Pass.
